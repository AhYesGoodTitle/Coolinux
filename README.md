Make sure the filesystem and shell files are in the same folder, then run shell.py in your terminal of choice :)

This is in VERY early development. 
Part of my mark for coding class is making a git page, so don't expect clean (or working :p ) code.
